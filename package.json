{
  "name": "agent-development-lab",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run dev:web",
    "dev:web": "bun run --cwd apps/web dev",
    "dev:docs": "bun run --cwd apps/docs dev",
    "build": "bun run build:packages && bun run build:web && bun run build:docs",
    "build:packages": "bun run --cwd packages/common build && bun run --cwd packages/runtime build",
    "build:web": "bun run --cwd apps/web build",
    "build:docs": "bun run --cwd apps/docs build",
    "lint": "eslint .",
    "typecheck": "bun run --cwd apps/web typecheck && bun run --cwd apps/docs typecheck && bun run --cwd packages/runtime typecheck && bun run --cwd packages/common typecheck",
    "test": "bun run --cwd packages/runtime test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/bun": "^1.3.3",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "nitro": "npm:nitro-nightly@latest",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.46.4"
  }
}

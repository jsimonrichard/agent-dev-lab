---
"@agent-dev-lab/web": patch
---

Start-workflow dialog can edit the entire input object as a document/JSON payload (not only nested `kind:json` fields). Object field names stay fully visible when defaults are long, and the dialog is widened for the inline editor.

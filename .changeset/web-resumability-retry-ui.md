---
"@agent-dev-lab/web": minor
"@agent-dev-lab/core": patch
---

Inspection UI for attempt-lineage resumability: Retry / Retry from here (including waterfall and step-list context menus), sidebar refresh when a new attempt starts, copied/replayed steps and nested runs marked with a link back to the original, and `WorkflowStore.listStepRecords` so seeded replay prefix steps render before skip events arrive.

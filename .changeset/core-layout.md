---
"@agent-dev-lab/core": patch
---

Core package layout for the first release: runtime implementation in `adl-runtime-impl.ts`, message stores under `stores/`, and prompt templates under `template/`.

---
"@agent-dev-lab/core": patch
---

Record each episode's raw `toolProviderContext` on `agent_started` and project it onto `adl_agent_episodes` / `AgentEpisodeSummary` for inspection and fork seeding.

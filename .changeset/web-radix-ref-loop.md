---
"@agent-dev-lab/web": patch
---

Bump `radix-ui` to ^1.6.7 so FocusScope/DismissableLayer compose refs stay stable under React 19 (stops the setRef Maximum update depth variant on ScrollArea / menus / context menus).

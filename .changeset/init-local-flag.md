---
"@agent-dev-lab/cli": patch
---

Add `adl init --local` for framework development: pin generated `@agent-dev-lab/*` deps to this checkout with `file:` + overrides. The flag is hidden in published CLI help.

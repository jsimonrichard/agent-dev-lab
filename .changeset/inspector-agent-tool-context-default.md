---
"@agent-dev-lab/core": patch
"@agent-dev-lab/web": patch
---

Add inspector-only per-agent default `toolProviderContext` (`adl_inspector_agent_settings`) and a definition-page editor. `agent.run` never reads it.

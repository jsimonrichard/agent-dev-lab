---
"@agent-dev-lab/web": patch
---

Stand-alone dashboard chats can supply `toolProviderContext` from a ToolProvider's `contextSchema` (or a JSON field) instead of only workflow-driven `agent.run` calls.

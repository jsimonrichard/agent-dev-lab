---
"@agent-dev-lab/web": patch
---

Inspection UI JSON fields use a structured document editor (add/remove array items and object keys) with a raw JSON escape hatch that fails closed on invalid paste.

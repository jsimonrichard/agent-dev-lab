---
"@agent-dev-lab/web": patch
---

Fix the agent settings panel: `stopWhen` is labeled default vs custom from `agent.definition.stopWhen`, and a `ToolProvider`'s tools come from `listTools()` (empty if missing).

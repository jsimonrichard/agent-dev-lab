---
"@agent-dev-lab/core": patch
"@agent-dev-lab/web": patch
---

Nested runs show in the tree and waterfall under the calling step, or under the workflow row when called at the root. `parentStepId` is set when the call is inside `ctx.step`.

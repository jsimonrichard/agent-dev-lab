---
"@agent-dev-lab/web": patch
"@agent-dev-lab/cli": patch
"@agent-dev-lab/core": patch
---

`adl dashboard` now watches the project registry. `--serve` disables watching (`ADL_PROJECT_WATCH=0`); `--prebuilt` forces the Nitro UI in the monorepo.

---
"@agent-dev-lab/core": patch
---

Bump the `zod` dependency to `^4.1.8` (the `ai` / `@ai-sdk/openai` versions already in use support either 3 or 4 as a peer). Projects still on Zod 3 for their own workflow/agent/template schemas will need to upgrade to Zod 4 alongside this release.

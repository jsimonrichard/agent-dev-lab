# Copy to `.env` (or `.env.local`) and fill in your key.
# Real environment variables take precedence over these files.

# Required to run the AI workflows (write-article, answer-question).
OPENAI_API_KEY=sk-...

# Optional: override the default model (default: gpt-5.4-mini).
# ADL_OPENAI_MODEL=gpt-5.5
